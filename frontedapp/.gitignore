
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# ── Expo generated native directories (managed workflow — EAS rebuilds these) ─
android/
ios/
.expo/

# ── Environment variable files ──────────────────────────────────────────────
# .env.local and .env.*.local hold machine-specific overrides (e.g. local IP)
# and must never be committed.
.env.local
.env.*.local
